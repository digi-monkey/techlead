# demo-topk

Minimal demo project for testing the techlead iteration workflow.

## Task
Optimize `findTopKFrequent(nums, k)` in `src/topk.js`.

Current implementation is intentionally slow.

## Local commands
- `npm test`
- `npm run bench`

## Suggested Goal text for init
Reduce average benchmark latency to under 300ms while keeping tests green.
