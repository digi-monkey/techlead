const I18N = {
  en: {
    "meta.title": "VA Auto-Pilot | Goal-First Autonomous Engineering Loop",
    "meta.description":
      "VA Auto-Pilot is a goal-first autonomous engineering loop: define outcomes, constraints, and acceptance, then let frontier models execute and self-verify.",
    "meta.ogDescription":
      "Do not micromanage prompts. Set the highest bar and constraints, then let frontier models run a closed-loop verification system.",
    "meta.twitterDescription":
      "Goal-first autonomous engineering loop with CLI orchestration, quality gates, and verifiable outcomes.",

    "nav.philosophy": "Philosophy",
    "nav.loop": "Loop",
    "nav.commands": "Commands",
    "nav.compare": "Compare",

    "cta.repo": "Repository",
    "cta.install": "Get Skill",
    "cta.viewLoop": "View Interactive Loop",

    "hero.eyebrow": "Goal-First Autonomous Engineering",
    "hero.title": "Do not write implementation prompts. Set the bar, then let frontier models run.",
    "hero.lead":
      "VA Auto-Pilot delegates execution to top models and enforces deterministic quality gates so outcomes are auditable, reproducible, and resilient.",
    "hero.signal1": "No step-by-step micromanagement",
    "hero.signal2": "Agent dynamically parallelizes CLI agents",
    "hero.signal3": "Closed-loop verification system",

    "philosophy.eyebrow": "Design Philosophy",
    "philosophy.title": "A protocol, not a prompt template",
    "philosophy.card1.title": "Do not tell agents how",
    "philosophy.card1.body":
      "Define the highest standard, hard constraints, and measurable acceptance. Leave path planning to frontier models.",
    "philosophy.card2.title": "Manager agent over manual orchestration",
    "philosophy.card2.body":
      "Humans define destination and boundaries. The manager agent coordinates implementation agents through deterministic CLI actions and runtime concurrency decisions.",
    "philosophy.card3.title": "Closed-loop verification",
    "philosophy.card3.body":
      "Build, review, acceptance, and state updates are mandatory. Completion is machine-verified, not presentation-driven.",
    "philosophy.card4.title": "Model frontier by default",
    "philosophy.card4.body":
      "For high-leverage tasks, use the strongest model available. Better planning quality compounds over long loops.",
    "philosophy.card5.title": "Memory is explicit",
    "philosophy.card5.body":
      "State and run memory live in files, not assumptions. The loop remains recoverable after resets and handoffs.",

    "loop.eyebrow": "Interactive Loop",
    "loop.title": "State machine as an operational contract",
    "loop.detailKicker": "Current State",
    "loop.autoplayPause": "Pause Autoplay",
    "loop.autoplayPlay": "Resume Autoplay",

    "demo.eyebrow": "Animated Demo",
    "demo.title": "Watch one autonomous cycle",
    "demo.terminalTitle": "Execution Stream",
    "demo.run": "Run Again",
    "demo.promptKicker": "Goal-First Directive Example",

    "commands.eyebrow": "Distribution & Commands",
    "commands.title": "Install once, delegate continuously",
    "commands.skillDirLabel": "Skill directory (for Codex skill-installer)",
    "commands.skillRawLabel": "Direct skill URL (for any agent)",
    "commands.codexInstall": "Codex install",
    "commands.codexUse": "Codex invocation",
    "commands.claudeInstall": "Claude install",
    "commands.claudeUse": "Claude invocation",

    "compare.eyebrow": "Positioning",
    "compare.title": "Why VA Auto-Pilot exists",
    "compare.va.title": "VA Auto-Pilot",
    "compare.va.body":
      "Best for teams that need governance, deterministic gates, and recoverable long-running execution loops.",
    "compare.ralph.title": "Ralph-style loop",
    "compare.ralph.body":
      "Great for rapid small-task iteration with minimal ceremony and lightweight setup.",
    "compare.link": "Read full comparison",

    "credits.eyebrow": "Credits",
    "credits.title": "Built by Vadaski & Codex",
    "credits.body":
      "Authors: Vadaski, Codex. Acknowledgements: Claude and the Vera project, where many practical patterns were distilled and validated.",

    "footer.tagline": "Set standards. Constrain risk. Let frontier models execute.",

    "common.copy": "Copy",
    "common.copyLink": "Copy Link",
    "common.copyCmd": "Copy Command",
    "common.copied": "Copied",
    "common.copyFail": "Copy failed"
  },
  zh: {
    "meta.title": "VA Auto-Pilot | 目标优先的自治工程闭环",
    "meta.description":
      "VA Auto-Pilot 是目标优先的自治工程闭环：定义目标、约束与验收，把实现路径交给最前沿模型并进行自验证。",
    "meta.ogDescription":
      "不要微操提示词。给最高标准与边界，让最前沿模型在闭环验证系统中自主执行。",
    "meta.twitterDescription": "目标优先的自治工程系统：CLI 编排、质量门禁、可验证交付。",

    "nav.philosophy": "设计哲学",
    "nav.loop": "闭环",
    "nav.commands": "命令",
    "nav.compare": "对比",

    "cta.repo": "仓库",
    "cta.install": "获取 Skill",
    "cta.viewLoop": "查看交互闭环",

    "hero.eyebrow": "目标优先的自治工程",
    "hero.title": "不要自己写实现提示词。给标准，让最前沿模型执行。",
    "hero.lead": "VA Auto-Pilot 把执行委派给顶尖模型，并通过确定性门禁强制收敛，让结果可审计、可复现、可恢复。",
    "hero.signal1": "不做步骤级微操",
    "hero.signal2": "Agent 动态并发调度 CLI Agent",
    "hero.signal3": "闭环验证系统",

    "philosophy.eyebrow": "设计哲学",
    "philosophy.title": "这是一套协议，不是提示词模板",
    "philosophy.card1.title": "不要告诉 Agent 如何做",
    "philosophy.card1.body": "只定义最高标准、硬约束和可测验收，把路径规划交给最前沿模型。",
    "philosophy.card2.title": "管理 Agent，而非手动编排",
    "philosophy.card2.body": "人类定义终局与边界，管理 Agent 通过 CLI 协同执行 Agent，并在运行时动态决定并发策略。",
    "philosophy.card3.title": "闭环验证",
    "philosophy.card3.body": "Build、Review、Acceptance、状态推进必须全走完，完成由机器判定，不靠主观感受。",
    "philosophy.card4.title": "默认使用最强模型",
    "philosophy.card4.body": "高杠杆任务优先上最强模型，长期迭代里规划质量会持续复利。",
    "philosophy.card5.title": "记忆显式化",
    "philosophy.card5.body": "状态与运行记忆写入文件，不依赖临时上下文，重启与交接后仍可恢复。",

    "loop.eyebrow": "交互状态机",
    "loop.title": "状态机是执行契约",
    "loop.detailKicker": "当前状态",
    "loop.autoplayPause": "暂停自动播放",
    "loop.autoplayPlay": "继续自动播放",

    "demo.eyebrow": "动画演示",
    "demo.title": "一轮自治执行示例",
    "demo.terminalTitle": "执行流",
    "demo.run": "重新演示",
    "demo.promptKicker": "目标优先指令示例",

    "commands.eyebrow": "分发与命令",
    "commands.title": "一次安装，持续委派",
    "commands.skillDirLabel": "Skill 目录链接（Codex skill-installer）",
    "commands.skillRawLabel": "Skill 直链（任意 Agent）",
    "commands.codexInstall": "Codex 安装",
    "commands.codexUse": "Codex 调用",
    "commands.claudeInstall": "Claude 安装",
    "commands.claudeUse": "Claude 调用",

    "compare.eyebrow": "定位",
    "compare.title": "为什么是 VA Auto-Pilot",
    "compare.va.title": "VA Auto-Pilot",
    "compare.va.body": "适合需要流程治理、确定性门禁、可恢复长周期执行的团队与项目。",
    "compare.ralph.title": "Ralph 风格",
    "compare.ralph.body": "适合低流程负担的小任务高速迭代与原型冲刺。",
    "compare.link": "阅读完整对比",

    "credits.eyebrow": "作者与致谢",
    "credits.title": "由 Vadaski 与 Codex 共创",
    "credits.body": "作者：Vadaski、Codex。致谢：Claude 与 Vera 项目，许多可复用工程模式在其中被沉淀并验证。",

    "footer.tagline": "定义标准，约束风险，让最前沿模型执行。",

    "common.copy": "复制",
    "common.copyLink": "复制链接",
    "common.copyCmd": "复制命令",
    "common.copied": "已复制",
    "common.copyFail": "复制失败"
  }
};

const STATE_DETAILS = {
  en: [
    {
      title: "Backlog",
      body: "Prioritized outcomes waiting for execution. The manager agent selects by priority and dependencies.",
      checks: ["Objective is explicit", "Constraints are complete", "Acceptance is measurable"]
    },
    {
      title: "In Progress",
      body: "Implementation runs under objective + constraints only. No step-by-step instructions are prescribed. Independent tracks can run in parallel.",
      checks: ["Parallelize independent tracks", "CLI-first execution", "Record key decisions"]
    },
    {
      title: "Review",
      body: "Independent review lenses evaluate risk, correctness, and architecture. Blocking findings must be fixed.",
      checks: ["Security lens", "QA lens", "Domain + Architecture lens"]
    },
    {
      title: "Testing",
      body: "Acceptance flows validate behavior and synchronize parallel tracks. MUST assertions are strict; SHOULD assertions enforce quality floor.",
      checks: ["MUST = 100%", "SHOULD >= 80%", "Parallel tracks synchronize at gates"]
    },
    {
      title: "Done",
      body: "Task is committed only after all required gates pass. State and journal are updated for the next cycle.",
      checks: ["Commit created", "State persisted", "Run memory appended"]
    }
  ],
  zh: [
    {
      title: "Backlog",
      body: "按优先级排好的目标队列。管理 Agent 按优先级与依赖关系选取任务。",
      checks: ["目标清晰", "约束完整", "验收可测"]
    },
    {
      title: "In Progress",
      body: "只基于目标与约束执行，不给实现步骤，避免人为锁死路径。独立轨道可并发推进。",
      checks: ["并发执行独立轨道", "CLI 优先", "记录关键决策"]
    },
    {
      title: "Review",
      body: "多视角独立审查风险、正确性与架构一致性，阻断问题必须先修复。",
      checks: ["安全视角", "QA 视角", "领域与架构视角"]
    },
    {
      title: "Testing",
      body: "验收流验证行为结果并同步并发轨道。MUST 严格通过，SHOULD 保证质量下限。",
      checks: ["MUST = 100%", "SHOULD >= 80%", "并发轨道在门禁处同步"]
    },
    {
      title: "Done",
      body: "全部门禁通过后才提交，同时更新状态和运行记忆，进入下一轮。",
      checks: ["已提交", "状态已持久化", "运行记忆已追加"]
    }
  ]
};

const DEMO_LINES = {
  en: [
    "$ va-auto-pilot",
    "[manager] Load human-board + run-journal",
    "[manager] next -> AP-014 start-task",
    "[delegation] objective: improve checkout reliability under 300ms p95",
    "[delegation] constraints: keep API boundary, no security regression",
    "[manager] spawn 3 parallel CLI tracks (api/ui/tests)",
    "[gate] npm run check:all ... PASS",
    "[gate] parallel outputs synchronized",
    "[gate] codex review --uncommitted ... PASS",
    "[gate] npm run validate:distribution ... PASS",
    "[state] AP-014 -> Done",
    "[journal] appended: failure mode + mitigation pattern",
    "[loop] continue with next highest priority task"
  ],
  zh: [
    "$ va-auto-pilot",
    "[manager] 读取 human-board + run-journal",
    "[manager] next -> AP-014 start-task",
    "[委派] 目标：在 p95<300ms 下提升结算稳定性",
    "[委派] 约束：不改 API 边界，不引入安全回归",
    "[manager] 启动 3 条并发轨道（api/ui/tests）",
    "[门禁] npm run check:all ... PASS",
    "[门禁] 并发输出已同步",
    "[门禁] codex review --uncommitted ... PASS",
    "[门禁] npm run validate:distribution ... PASS",
    "[状态] AP-014 -> Done",
    "[日志] 已追加：故障模式与修复模式",
    "[循环] 进入下一优先级任务"
  ]
};

const GOAL_PROMPTS = {
  en: `$va-auto-pilot

Objective:
Launch onboarding v2 that improves activation by >= 12%.

Constraints:
- Keep architecture boundaries unchanged
- No security regressions
- Keep p95 response under 300ms

Acceptance:
- typecheck/lint/test pass
- review has no blocking findings
- acceptance flow MUST 100%, SHOULD >= 80%`,
  zh: `$va-auto-pilot

目标：
上线 onboarding v2，激活率提升 >= 12%。

约束：
- 不改变架构边界
- 不引入安全回归
- p95 响应保持在 300ms 内

验收：
- typecheck/lint/test 全通过
- review 无阻断问题
- 验收流 MUST 100%，SHOULD >= 80%`
};

let currentLang = localStorage.getItem("va_auto_pilot_lang") === "zh" ? "zh" : "en";
let activeState = 0;
let stateAutoplayTimer = null;
let stateAutoplay = true;

function readMeta(name) {
  return document.querySelector(`meta[name="${name}"]`)?.content?.trim() ?? "";
}

function inferGitHubContext() {
  const host = window.location.hostname;
  if (!host.endsWith("github.io")) {
    return { owner: "", repo: "" };
  }

  const owner = host.split(".")[0] ?? "";
  const firstPath = window.location.pathname.split("/").filter(Boolean)[0] ?? "";
  const repo = firstPath || `${owner}.github.io`;
  return { owner, repo };
}

function toRawUrl(owner, repo, branch, filePath) {
  return `https://raw.githubusercontent.com/${owner}/${repo}/${branch}/${filePath}`;
}

function toTreeUrl(owner, repo, branch, filePath) {
  return `https://github.com/${owner}/${repo}/tree/${branch}/${filePath}`;
}

function toBlobUrl(owner, repo, branch, filePath) {
  return `https://github.com/${owner}/${repo}/blob/${branch}/${filePath}`;
}

function setText(id, value) {
  const node = document.getElementById(id);
  if (!node) return;
  node.textContent = value;
}

function setLink(id, href) {
  const node = document.getElementById(id);
  if (!node) return;
  node.href = href;
  node.textContent = href;
}

function applyI18n(lang) {
  const dict = I18N[lang];
  if (!dict) return;

  for (const node of document.querySelectorAll("[data-i18n]")) {
    const key = node.getAttribute("data-i18n");
    if (!key || dict[key] === undefined) continue;
    node.textContent = dict[key];
  }

  document.documentElement.lang = lang === "zh" ? "zh-CN" : "en";
  document.title = dict["meta.title"];

  const description = document.querySelector('meta[name="description"]');
  if (description) description.setAttribute("content", dict["meta.description"]);

  const ogDescription = document.querySelector('meta[property="og:description"]');
  if (ogDescription) ogDescription.setAttribute("content", dict["meta.ogDescription"]);

  const ogTitle = document.querySelector('meta[property="og:title"]');
  if (ogTitle) ogTitle.setAttribute("content", dict["meta.title"]);

  const twitterDesc = document.querySelector('meta[name="twitter:description"]');
  if (twitterDesc) twitterDesc.setAttribute("content", dict["meta.twitterDescription"]);

  const twitterTitle = document.querySelector('meta[name="twitter:title"]');
  if (twitterTitle) twitterTitle.setAttribute("content", dict["meta.title"]);

}

function setupLanguageToggle() {
  const en = document.getElementById("langEn");
  const zh = document.getElementById("langZh");
  if (!en || !zh) return;

  function applyButtons(lang) {
    en.classList.toggle("active", lang === "en");
    zh.classList.toggle("active", lang === "zh");
  }

  en.addEventListener("click", () => {
    currentLang = "en";
    localStorage.setItem("va_auto_pilot_lang", "en");
    applyI18n(currentLang);
    renderDynamicContent();
    renderStateDetail(activeState);
    runDemoAnimation();
    applyButtons(currentLang);
  });

  zh.addEventListener("click", () => {
    currentLang = "zh";
    localStorage.setItem("va_auto_pilot_lang", "zh");
    applyI18n(currentLang);
    renderDynamicContent();
    renderStateDetail(activeState);
    runDemoAnimation();
    applyButtons(currentLang);
  });

  applyButtons(currentLang);
}

async function copyText(value) {
  if (!value) return false;
  try {
    await navigator.clipboard.writeText(value);
    return true;
  } catch {
    return false;
  }
}

function bindCopyButtons() {
  for (const button of document.querySelectorAll(".copy-btn")) {
    button.addEventListener("click", async () => {
      const targetId = button.getAttribute("data-copy-target");
      if (!targetId) return;
      const text = document.getElementById(targetId)?.textContent?.trim() ?? "";
      const ok = await copyText(text);
      const dict = I18N[currentLang];
      const original = button.textContent;
      button.textContent = ok ? dict["common.copied"] : dict["common.copyFail"];
      window.setTimeout(() => {
        button.textContent = original;
      }, 1200);
    });
  }

  const copySkillLink = document.getElementById("copySkillLink");
  const copySkillRawLink = document.getElementById("copySkillRawLink");

  if (copySkillLink) {
    copySkillLink.addEventListener("click", async () => {
      const ok = await copyText(document.getElementById("skillDirLink")?.textContent ?? "");
      const dict = I18N[currentLang];
      const original = copySkillLink.textContent;
      copySkillLink.textContent = ok ? dict["common.copied"] : dict["common.copyFail"];
      window.setTimeout(() => {
        copySkillLink.textContent = original;
      }, 1200);
    });
  }

  if (copySkillRawLink) {
    copySkillRawLink.addEventListener("click", async () => {
      const ok = await copyText(document.getElementById("skillRawLink")?.textContent ?? "");
      const dict = I18N[currentLang];
      const original = copySkillRawLink.textContent;
      copySkillRawLink.textContent = ok ? dict["common.copied"] : dict["common.copyFail"];
      window.setTimeout(() => {
        copySkillRawLink.textContent = original;
      }, 1200);
    });
  }
}

function setupTabs() {
  const tabs = Array.from(document.querySelectorAll(".tab-btn"));
  const panels = Array.from(document.querySelectorAll(".tab-panel"));

  for (const tab of tabs) {
    tab.addEventListener("click", () => {
      const target = tab.getAttribute("data-tab");
      if (!target) return;

      for (const item of tabs) {
        item.classList.toggle("active", item === tab);
      }

      for (const panel of panels) {
        panel.classList.toggle("active", panel.getAttribute("data-panel") === target);
      }
    });
  }
}

function setupReveal() {
  const nodes = document.querySelectorAll(".reveal");
  const observer = new IntersectionObserver(
    (entries) => {
      for (const entry of entries) {
        if (!entry.isIntersecting) continue;
        entry.target.classList.add("visible");
        observer.unobserve(entry.target);
      }
    },
    { threshold: 0.14 }
  );

  for (const node of nodes) {
    observer.observe(node);
  }
}

function renderStateDetail(index) {
  const bundle = STATE_DETAILS[currentLang][index];
  const title = document.getElementById("stateDetailTitle");
  const body = document.getElementById("stateDetailBody");
  const checks = document.getElementById("stateDetailChecks");
  if (!bundle || !title || !body || !checks) return;

  title.textContent = bundle.title;
  body.textContent = bundle.body;
  checks.innerHTML = "";
  for (const item of bundle.checks) {
    const li = document.createElement("li");
    li.textContent = item;
    checks.append(li);
  }

  for (const node of document.querySelectorAll(".state-node")) {
    const nodeIndex = Number(node.getAttribute("data-state"));
    node.classList.toggle("active", nodeIndex === index);
  }
}

function stepState() {
  activeState = (activeState + 1) % STATE_DETAILS[currentLang].length;
  renderStateDetail(activeState);
}

function startAutoplay() {
  if (stateAutoplayTimer) window.clearInterval(stateAutoplayTimer);
  stateAutoplayTimer = window.setInterval(stepState, 2200);
}

function setupStateMachine() {
  for (const node of document.querySelectorAll(".state-node")) {
    node.addEventListener("click", () => {
      activeState = Number(node.getAttribute("data-state") ?? 0);
      renderStateDetail(activeState);
    });
  }

  const toggle = document.getElementById("machineToggle");
  if (toggle) {
    toggle.addEventListener("click", () => {
      stateAutoplay = !stateAutoplay;
      const dict = I18N[currentLang];

      if (stateAutoplay) {
        startAutoplay();
        toggle.textContent = dict["loop.autoplayPause"];
      } else {
        if (stateAutoplayTimer) window.clearInterval(stateAutoplayTimer);
        toggle.textContent = dict["loop.autoplayPlay"];
      }
    });
  }

  renderStateDetail(activeState);
  startAutoplay();
}

function renderDynamicContent() {
  const ownerMeta = readMeta("github-owner");
  const repoMeta = readMeta("github-repo") || "va-auto-pilot";
  const branch = readMeta("github-branch") || "main";
  const inferred = inferGitHubContext();

  const owner = ownerMeta || inferred.owner || "Vadaski";
  const repo = repoMeta || inferred.repo || "va-auto-pilot";

  const repoUrl = `https://github.com/${owner}/${repo}`;
  const skillDirUrl = toTreeUrl(owner, repo, branch, "skills/va-auto-pilot");
  const skillRawUrl = toRawUrl(owner, repo, branch, "skills/va-auto-pilot/SKILL.md");
  const claudeCommandRawUrl = toRawUrl(owner, repo, branch, "skills/va-auto-pilot/claude-command.md");
  const compareDocPath =
    currentLang === "zh"
      ? "docs/comparisons/va-auto-pilot-vs-ralph.zh.md"
      : "docs/comparisons/va-auto-pilot-vs-ralph.en.md";
  const compareDocUrl = toBlobUrl(owner, repo, branch, compareDocPath);

  setLink("skillDirLink", skillDirUrl);
  setLink("skillRawLink", skillRawUrl);

  const repoLink = document.getElementById("repoLink");
  if (repoLink) repoLink.href = repoUrl;

  const footerRepoLink = document.getElementById("footerRepoLink");
  if (footerRepoLink) footerRepoLink.href = repoUrl;
  const compareDocLink = document.getElementById("compareDocLink");
  if (compareDocLink) compareDocLink.href = compareDocUrl;

  setText("codexInstallCmd", `$skill-installer install ${skillDirUrl}`);
  setText(
    "codexUseCmd",
    currentLang === "zh"
      ? "$va-auto-pilot 在当前仓库按最高标准执行一轮闭环，不要我给实现步骤"
      : "$va-auto-pilot run one full loop in this repo with highest standards; do not ask me for implementation steps"
  );

  setText(
    "claudeInstallCmd",
    `mkdir -p .claude/commands\ncurl -fsSL ${claudeCommandRawUrl} -o .claude/commands/va-auto-pilot.md`
  );
  setText("claudeUseCmd", "/va-auto-pilot");

  setText("goalPromptExample", GOAL_PROMPTS[currentLang]);
}

function runDemoAnimation() {
  const log = document.getElementById("demoLog");
  if (!log) return;

  const lines = DEMO_LINES[currentLang];
  log.textContent = "";

  let index = 0;
  function writeNext() {
    if (index >= lines.length) return;
    log.textContent += `${lines[index]}\n`;
    log.scrollTop = log.scrollHeight;
    index += 1;
    window.setTimeout(writeNext, 260);
  }

  writeNext();
}

function setupDemoButton() {
  const button = document.getElementById("runDemo");
  if (!button) return;
  button.addEventListener("click", runDemoAnimation);
}

function boot() {
  applyI18n(currentLang);
  setupLanguageToggle();
  setupTabs();
  setupReveal();
  setupStateMachine();
  setupDemoButton();
  renderDynamicContent();
  bindCopyButtons();
  runDemoAnimation();
}

boot();
